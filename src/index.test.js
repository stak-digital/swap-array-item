import expect from 'expect';
import swapArrayItems from './index';

describe('swapArrayItems', () => {

	it('should return hello world', () => {
		expect(swapArrayItems()).toBe('hello world');
	});

});